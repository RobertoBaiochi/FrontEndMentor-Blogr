import { DefaultTheme } from 'styled-components/dist/types';

export const theme = {
  media: {
    tabletBreackpoint: '(min-width: 567px)',
    desktopBreackpoint: '(min-width: 913px)',
  },
} as const;